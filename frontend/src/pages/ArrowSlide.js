import styles from "./ArrowSlide.module.css";

const ArrowSlide = () => {
  return (
    <div className={styles.arrowSlide}>
      <img className={styles.vectorIcon} alt="" src="../vector.svg" />
    </div>
  );
};

export default ArrowSlide;
